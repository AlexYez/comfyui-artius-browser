Mode: HIGH-PERFORMANCE

- Prioritize correctness and stability over token savings.
- Do not guess — verify in code or ask.

Navigation:
- Start with targeted search (symbol/function/class).
- Read minimal code (<=120 lines).
- Expand context only if required.
- Avoid rereading the same code.

Editing:
- Make the smallest correct and safe patch.
- Avoid broad refactors unless required.
- Respect existing architecture.

Validation:
- Run only minimal checks needed to prove correctness.